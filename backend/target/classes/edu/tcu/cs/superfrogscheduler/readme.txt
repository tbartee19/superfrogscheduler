Here the main java file is created to run the program as well as directories for everything we'd need

These include controllers (routers) for making api calls from the frontend to the backend separated by the types of requests
we'd make, as well as a system folder for storing configuration files and such 
